import { Box } from "@chakra-ui/react"

/**
 * Displays the balance of a wallet address
 * @returns 
 */
export const TokenBalance = ({ tokenAddress, walletAddress, chain, variant, theme }) => {
  return (
    <Box>
      
    </Box>
  )
}