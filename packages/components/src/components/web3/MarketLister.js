import { Box } from "@chakra-ui/react"

/**
 * Lists an asset to a marketplace contract
 * @returns 
 */
export const MarketLister = ({ providerUrl }) => {
  
  return (
    <Box>
      MarketLister
    </Box>
  )
}