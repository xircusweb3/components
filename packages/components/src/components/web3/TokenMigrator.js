import { Box } from "@chakra-ui/react"

/**
 * Views an NFT Item
 * @returns 
 */
export const NFTViewer = ({ assetAddr, assetId, assetType, chain, variant, theme }) => {
  return (
    <Box>

    </Box>
  )
}