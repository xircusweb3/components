import { Box } from "@chakra-ui/react"

/**
 * Returns a panel for buying credit listing topups on a marketplace
 * @returns 
 */
export const MarketTopup = () => {
  return (
    <Box>
      MarketTopup
    </Box>
  )
}