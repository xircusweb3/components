import { Box } from "@chakra-ui/react"

/**
 * Views an NFT Item
 * @returns 
 */
export const NFTBase = ({ assetAddr, assetId, assetType, chain, variant, theme }) => {
  return (
    <Box>

    </Box>
  )
}

export const NFTOpenSea = ({ assetAddr, assetId, assetType, chain, variant, theme }) => {
  return (
    <Box>

    </Box>
  )
}

export const NFTRarible = ({ assetAddr, assetId, assetType, chain, variant, theme }) => {
  return (
    <Box>

    </Box>
  )
}