import { Box } from "@chakra-ui/react"

/**
 * Displays all deployed apps from provider endpoint
 * Includes search and card resizing
 * @returns 
 */
export const AppGrid = ({ providerUrl, variant, theme, onFilter }) => {
  return (
    <Box>

    </Box>
  )
}